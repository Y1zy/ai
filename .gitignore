.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.wheel_cache/
*.egg-info/
*.log
*.err.log

# Local builds and validation artifacts
overlay_cs/bin/
qa/
.build-check/
.runtime/
dist/

# User-local settings and secrets (normally stored outside the repository)
# 这些文件实际存放在 %LOCALAPPDATA%\WasapiParaformerOverlay\，但一并忽略以防
# 被误复制到项目目录后提交（vision.key 含加密的视觉模型 Key，
# knowledge.json 含用户简历/话术，phone_share.json 含局域网配对令牌）。
config.json
deepseek.key
vision.key
phone_share.json
knowledge.json
.env
.env.*

# Editors and operating systems
.vs/
.vscode/
.idea/
Thumbs.db
.DS_Store

# Local security-scan plugin state
.mimosa/
