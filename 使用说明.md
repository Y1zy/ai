# VoxRibbon 使用说明（含新功能：共享屏幕隐形 + 手机投屏）

## 一、首次安装（只做一次）

1. 项目当前电脑没有 Python 3.11，先装（PowerShell 执行）：
   ```powershell
   winget install -e --id Python.Python.3.11
   ```
   装完关闭并重开 PowerShell。

2. 打开项目文件夹（资源管理器地址栏输入 `powershell` 回车），执行：
   ```powershell
   Set-ExecutionPolicy -Scope Process Bypass
   .\install.ps1 -Cpu     # 没有 NVIDIA 显卡用这条
   .\install.ps1 -Cuda    # 有 NVIDIA 显卡用这条（更快）
   ```
   看到「安装和自检完成」即可。首次启动还会联网下载 Paraformer 语音识别模型。

## 二、日常启动（一条命令搞定）

```powershell
.\launch.ps1
```

`launch.ps1` 会一次性把 **ASR 服务 + 手机投屏 + 桌面字幕悬浮窗** 全部拉起（后台运行，健康检查就绪后才报"VoxRibbon 已就绪"）。

- 默认**开启手机投屏**；不需要手机功能时用 `.\launch.ps1 -NoPhone`
- 录演示视频想让字幕可见：`.\launch.ps1 -AllowCapture`
- 打开 AI 设置：`.\launch.ps1 -AISettings`

启动后：
- 播放视频/语音 → 桌面出现实时字幕
- 打开 http://127.0.0.1:8765/settings 管理所有设置

## 三、共享屏幕隐形（默认已开启 ✅）

- **什么都不用设置**：字幕窗、设置窗、位置锁图标都从所有采集路径消失
  —— 腾讯会议/Zoom/Teams 共享屏幕、OBS 录屏、Win+Shift+S 截图都看不到，
  你自己屏幕上照常显示。
- **验证**：
  ```powershell
  .\overlay_cs\bin\OverlayCapture.exe out.png 0 --capture-affinity
  ```
  输出 `affinity=17` = 已隐形。
- **录演示视频想让它可见**：
  ```powershell
  .\start_overlay.ps1 -AllowCapture     # 只影响本次运行
  ```
  或设置窗取消勾选「共享屏幕 / 录屏时隐藏本窗口」（会记住）。
- **隐藏/显示字幕**：Ctrl+Alt+H（老板键，冲突时自动回退 Ctrl+Shift+H）
- **编辑/解锁**：Ctrl+Alt+O（冲突时 Ctrl+Shift+O）
- **移动字幕位置**：Ctrl+Alt+方向键 每次移动 30px（上/下/左/右；冲突时自动回退 Ctrl+Shift+方向键），位置自动保存
- **截图解题**：Ctrl+Shift+C（冲突时 Ctrl+Alt+C；需先在设置页启用截图解题，见第五节）

## 四、手机投屏（扫码 + 截图 + 保存图片 + 剪贴板同步）

### 首次配对
1. 用 `.\start.ps1 -SharePhone` 启动（会开放局域网，防火墙弹窗选「允许」）
2. 浏览器打开 http://127.0.0.1:8765/settings
3. 找到「📱 手机投屏（扫码配对）」卡片 → 勾选「启用手机投屏」→ 重启 start.ps1
4. 刷新页面，手机和电脑连**同一个 WiFi**（推荐手机开热点、电脑连热点）
5. 手机相机扫页面上的二维码

### 手机页面功能
| 按钮 | 作用 |
|---|---|
| 📸 立即截屏 | 电脑立刻截屏推到手机 |
| 💾 保存图片 | 下载当前截图为 JPG（也可长按图片保存） |
| 自动刷新 | 每 3 秒自动截屏 |
| 字幕/AI | 实时看字幕和 AI 流式回答 |
| 📋 电脑剪贴板 | 电脑 Ctrl+C 后这里出现，点「复制到手机」 |
| 把手机剪贴板发到电脑 | 一键读取手机剪贴板 → 电脑直接 Ctrl+V |

- 剪贴板同步说明：电脑 Ctrl+C 后手机自动收到（无需操作）；手机→电脑需点一次「把手机剪贴板发到电脑」按钮——手机浏览器不允许网页后台读取剪贴板，这是系统安全限制，已做到一键完成
- 换手机/怕泄露：设置页点「重新生成配对码」，旧二维码立刻作废

## 五、AI 回答（可选，任意 OpenAI 兼容接口）

设置页 → AI 助手（OpenAI 兼容格式）卡片：

1. 填 API Key（DeepSeek / 通义 / GLM / OpenAI 等均可）
2. 接口地址 BaseURL：默认 DeepSeek 官方；用其他供应商时改成对应地址（不含 `/chat/completions`），例如通义 `https://dashscope.aliyuncs.com/compatible-mode/v1`
3. 模型：可直接输入任意模型名（预设下拉含 deepseek-v4-flash / deepseek-v4-pro）
4. 勾选启用 → 保存并测试。不填也能用，字幕和投屏功能不受影响

提示词两级自定义（设置页和桌面设置窗都可改）：

| 字段 | 作用 |
|---|---|
| 完全自定义系统提示词 | 非空时整体替换内置模式提示词（简历/JD/公司上下文仍会附加） |
| 附加提示词 | 在现有提示词后追加要求，如「回答控制在 50 字以内」 |

### 🧪 AI 回答测试（不用等语音触发）

想快速检验当前 AI 的回答效果：

- **web 设置页**：AI 卡片 → 「回答测试」输入问题 → 点「发送测试问题」，显示回答全文 + 中转站**实际路由的模型名** + 耗时
- **桌面设置窗**（Ctrl+Alt+O → 翻译/AI 页）：同样的输入框和按钮，改完配置立即可测

测试走的是和字幕 AI 完全相同的管线（同一套提示词、简历/JD 上下文），测出来的就是实际效果。注意 web 页测试需要系统提示词就绪（在桌面设置窗发过一次测试或触发过一次字幕 AI 后可用）。

### 📸 截图解题（可选）

设置页 → 截图解题卡片：填视觉模型 BaseURL / 模型名 / API Key → 勾选启用。之后按 **Ctrl+Shift+C**（或手机页「🧠 截题+回答」按钮）即可截屏解题，答案流式打字显示。「解题提示词」可自定义，留空用内置默认。

## 五点五、面试记录（自动暂存 + 一键保存）

面试过程中，字幕、AI 回答、截图解题（含题图）会**自动暂存在内存里**（不写磁盘、不影响识别性能）。面试完到设置页「🗂 面试记录」卡片：

- **保存到本地**：写入 `%LOCALAPPDATA%\WasapiParaformerOverlay\records\日期-时间\`，内含 `transcript.md`（按时间线整理的字幕+AI 回答+解题，题图内嵌引用）和 `images\` 题图文件夹
- **打开记录文件夹**：直接在资源管理器里看
- **清空本场**：下一场面试前重置内存（服务重启也会自动清空）

## 六、常见问题

| 现象 | 处理 |
|---|---|
| 扫码后手机打不开 | 确认同一 WiFi；重启时用了 `-SharePhone`；防火墙放行 Python |
| 截图按钮没反应 | 电脑端没启用投屏（设置页勾选 + 重启） |
| 防截屏没生效 | Windows 版本需 ≥ Win10 2004；看 overlay.runtime.log 里 `applied=False` 即版本过低 |
| 字幕不见了 | 按 Ctrl+Alt+H 恢复 |
| AI 请求报错 | 检查 BaseURL 是否含 `/chat/completions`（不要带）；模型名是否拼写正确 |
